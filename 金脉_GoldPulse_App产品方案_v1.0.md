# 金脉 GoldPulse App 产品方案 v1.0（修订版）

> 修订说明：v1.0 原始版经调研同类开源项目（gold-price-monitor、Fin-Tracker 等）后全面修订。核心变更：
> 1. 明确免费行情数据源与降级策略（原"免费行情接口"未落实）
> 2. 资产计算模块重构：补入积存金生息、卖出手续费、平均成本摊薄规则
> 3. 新增交易时段状态机（Au9999 非 24 小时连续交易）
> 4. 明确 Android 本地价格提醒的交付机制与现实约束
> 5. 数据库设计重构：补主键、生息事件、唯一索引、卖出记录
> 6. 首次使用流程、数据备份、精度单位规范等产品细节补齐
> 7. 修正 MVP 阶段划分的矛盾（持仓录入必须持久化）

## 一、产品定位

**产品名称：金脉 GoldPulse**

定位：

> 我的黄金资产监控终端。

金脉 GoldPulse 是一款面向个人黄金投资者的本地化黄金行情与资产管理 App。

核心数据来源：

-   上海黄金交易所 Au9999 行情（通过免费行情接口获取）
-   用户自己的浙商积存金持仓数据（用户手动录入）

核心目标：

-   快速查看黄金涨跌
-   实时了解资产盈亏
-   管理黄金持仓成本
-   设置价格提醒

## 二、产品特点

-   本地运行
-   无服务器
-   无账号体系
-   数据保存在本机
-   免费行情接口
-   专注黄金资产管理

## 三、整体架构

    上海黄金交易所 Au9999 / 浙商积存金行情
                      |
              （免费行情接口：主源 + 备用源）
                      |
                 Flutter Android App
                      |
     ---------------------------------------------
     |           |            |                   |
    行情模块   计算模块     提醒模块          SQLite数据库
     |           |            |                   |
     |      （生息/手续费/    |             （本地持久化 +
     |       平均成本摊薄）    |              导出/导入备份）
     └──────────┴────────────┴───────────────────┘
                所有数据在本地完成处理

数据流设计要点：

1. 行情模块定时拉取 Au9999 / 积存金最新价 → 写入 `gold_price` 表（历史积累）
2. 计算模块基于 持仓表 + 交易记录表 → 实时计算当前价值、浮动收益、收益率
3. 提醒模块轮询最新价 → 命中条件则触发本地通知
4. 全部数据落本地 SQLite，支持一键导出/导入备份

## 四、技术方案

开发框架：

-   Flutter（仅 Android 端）

开发语言：

-   Dart

本地数据库：

-   SQLite（sqflite）+ 数据库迁移版本控制（sqflite_migration / 自建 version 管理）

状态管理：

-   Riverpod（Provider 亦可，建议 Riverpod：行情轮询 + 多页面共享持仓状态）
-   首次启动引导、持仓数据、提醒设置均为全局状态

网络请求：

-   Dio（带超时、重试、错误降级拦截器）

图表：

-   fl_chart（LineChart 走势图；K线使用 fl_chart 0.66+ 的 CandlestickChart，若不满足可换 k_chart / candlesticks 包）

通知：

-   flutter_local_notifications + android_alarm_manager / WorkManager（后台轮询提醒）

## 五、数据源方案（新增：原方案最大缺口）

### 主数据源：京东金融 / 京东黄金（免费，非官方）

调研自开源项目 gold-price-monitor（Electron 版金价监控），接口可用：

| 数据 | 接口 | 关键参数 |
|------|------|----------|
| 黄金实时价 | `https://api.jdjygold.com/gw2/generic/produTools/h5/m/getGoldPrice` | `goldCode=SGE-Au(T+D)` 等 |
| 浙商积存金最新价 | `https://api.jdjygold.com/gw2/generic/jrm/h5/m/stdLatestPrice` | `productSku=1961543816` |
| 简易报价 | `https://ms.jr.jd.com/gw2/generic/jdtwt/h5/m/getSimpleQuoteUseUniqueCodes` | `uniqueCodes=['CZB-JCJ']` |

积存金唯一码参考：

-   浙商积存金：`CZB-JCJ`（productSku `1961543816`）
-   民生积存金：`CMBC-JCJ`；工商：`ICBC-JCJ`；广发：`CGB-JCJ`；兴业：`CIB-JCJ`
-   Au9999 黄金 T+D：`SGE-Au(T+D)`

### 备用数据源（主源失效时降级）

-   新浪财经黄金接口（hq.sinajs.cn）
-   腾讯证券行情接口（qt.gtimg.cn）
-   上海黄金交易所官网（sge.com.cn，金价页面抓取，频率低）

### 数据源策略

1. 主源 → 备用源 → 本地缓存（上次成功数据 + 时间戳）三级降级
2. 每次成功请求缓存到 `gold_price` 表，接口全部失效时展示缓存并标注"离线数据"
3. 所有接口均为非官方、免费接口，**随时可能变更或失效**：请求参数与解析逻辑收敛到一个 `PriceApi` 适配层，单点维护，便于失效时快速切换
4. 刷新频率默认 2 分钟（参考项目默认 2s 对手机电量不友好），用户可配置 30s ~ 1 小时

## 六、App页面设计

### 1：首页 Dashboard

核心展示：

-   Au9999 当前价格
-   涨跌金额
-   涨跌百分比
-   浙商积存金收益
-   当前资产价值
-   目标价格距离

示例：

    金脉 GoldPulse

    Au9999

    780.20 元/g

    ▲ +3.50 (+0.45%)

    [交易中 / 已收盘 · 更新于 14:32:05]

    浙商积存金

    持仓 500.00g

    成本 618.53 元/g

    收益 +80,850 元  (+26.2%)

    目标距离：距 800 元/g 还差 19.80 元

### 2：行情页

功能：

-   实时黄金价格（Au9999 / 浙商积存金可切换）
-   涨跌趋势
-   历史走势
-   K线图

时间周期：

-   1日 / 7日 / 30日 / 1年（1年周期受本地历史数据积累长度限制，见"历史数据回填"）

### 3：资产页

功能：

-   持仓管理（Au9999 / 浙商积存金 两类账户）
-   交易记录（买入 / 卖出 / 生息，三类事件）
-   平均成本计算（自动摊薄生息克重）

支持：

    2025-01-01
    买入 100g @ 600元/g
    2025-03-15
    卖出 50g @ 720元/g（手续费 144 元）
    2025-06-30
    生息 +0.08g

### 4：提醒页

支持：

-   价格上涨提醒
-   价格下跌提醒
-   收益目标提醒

每条提醒含：类型、目标价、开关、触发次数、触发状态。

> 提醒机制的现实约束见"提醒模块"。

### 5：设置页

功能：

-   刷新频率设置
-   持仓修改
-   数据管理（导出 / 导入 / 清空）
-   界面设置（主题深色 / 亮色）
-   关于与免责声明

## 七、首页设计理念

设计目标：

打开 App 后 3 秒知道：

1.  黄金现在多少钱
2.  今天涨还是跌
3.  我的黄金赚多少钱

视觉优先级：

    黄金价格
        >
    涨跌幅
        >
    资产收益
        >
    目标距离

补充设计规范：

-   价格与收益使用大数字展示（≥ 36sp），支持自适应缩放避免溢出
-   金额千分位分组（80,850 元），克重保留 2 位小数展示、内部 4 位精度
-   涨跌红涨绿跌（符合国内习惯）；数据过期/离线时金色降级为灰色并标注时间戳
-   无持仓时展示引导空状态："点击添加你的第一笔黄金持仓"

## 八、核心功能模块

### 黄金行情模块

功能：

-   获取 Au9999 最新价格（主源 + 备用源降级）
-   获取涨跌幅（基于上日收盘价 preClose 计算）
-   保存历史行情到 `gold_price` 表（滚动保留最近 1 年，超期清理）
-   交易时段状态机（见下）

**交易时段状态机（新增）**

Au9999 非 24 小时连续交易，App 需识别以下状态并正确展示：

| 状态 | 时段（Au9999） | 界面表现 |
|------|----------------|----------|
| 交易中 | 日盘 9:00–11:30 / 13:30–15:30；夜盘 21:00–次日 2:30 | 正常刷新，绿点"交易中" |
| 午间休市 | 11:30–13:30 | 展示最新价，"午间休市" |
| 已收盘 | 15:30–21:00（夜盘前） | 展示当日收盘价，"已收盘" |
| 周末/节假日 | 周六 2:30 – 周一 9:00 | 展示上日收盘价，"休市" |

-   涨跌幅 =（当前价 − preClose）÷ preClose，preClose 为上一交易日的收盘价
-   积存金交易时段：周一 9:00 至周六凌晨 2:30，按银行公布为准
-   休市期间不主动轮询（省电），仅展示缓存；开盘自动恢复

**历史数据回填（新增）**

免费接口一般无法获取完整 1 年历史 K 线：

-   从首次启动起，行情模块持续采集，本地数据随时间增长
-   1 日 / 7 日 / 30 日周期优先可用；"1年"周期在本地积累不足 1 年时，按实际长度展示并标注起止日期
-   可选：首次启动时尝试从备用接口拉取一次近 30 日历史做初始回填，失败则静默跳过

### 资产计算模块（重构）

**浙商积存金业务规则（调研自银行公开计价规则）**

-   起购 1 克，克重按 0.0001 克递增
-   买入（认购）手续费 0%；卖出（赎回）手续费按赎回金额 0.4% 收取
-   每日按持仓克重生息（活期年化收益率 ÷ 360 × 克重，克重入账），生息克重会计入持仓、摊薄平均成本
-   平均成本 = 累计买入总成本 ÷ 当前持有克重（含生息克重）

**计算口径**

    当前价值 = 当前价格 × 当前持仓克重

    浮动收益 = 当前价值 − 累计买入总成本

    收益率 = 浮动收益 ÷ 累计买入总成本

    净收益（卖出结算）= 卖出克重 ×（卖出价 − 平均成本）− 卖出手续费

    平均成本 = 累计买入总成本 ÷ 当前持仓克重（随生息克重增加而摊薄）

**生息记录**

-   积存金生息按日入账，需要用户手动录入，或提供"按日估算"辅助（输入年化利率自动预先生息）
-   生息克重记录为 `type=interest` 的交易事件，参与持仓克重与平均成本计算

**示例**

    持仓：500g，买入总成本 310,000 元（平均 620 元/g）
    现价：781.50 元/g
    累计生息：+1.2g → 现持仓 501.2g

    当前价值 = 781.50 × 501.2 = 391,687.80 元
    浮动收益 = 391,687.80 − 310,000 = 81,687.80 元
    收益率 = 81,687.80 ÷ 310,000 = 26.35%

    若全部卖出（手续费 0.4%）：
    实得 = 391,687.80 × (1 − 0.004) = 390,121.05 元
    净收益 = 390,121.05 − 310,000 = 80,121.05 元

### 提醒模块

**支持类型**

    Au9999 价格 >= 800 元/g     → 价格上涨提醒
    Au9999 价格 <= 750 元/g     → 价格下跌提醒
    我的黄金资产 >= 40 万元      → 收益目标提醒

**Android 本地提醒的现实机制（新增，关键）**

纯本地 App 在应用被杀后无法"实时"推送。按可靠性分级：

1. 应用在前台 → 行情轮询命中条件 → 立即弹本地通知（可靠）
2. 应用在后台但进程存活 → 定时任务命中 → 通知（较可靠）
3. 应用被杀 → WorkManager 周期任务轮询（Android 最小周期约 15 分钟，**精度受限**，且国产 ROM 可能被清理后台，需引导用户加入白名单）
4. 可选升级方案 → 前台常驻通知服务（显示"监控中"，可靠性最高，但常驻通知有打扰性，作为用户可选项）

产品口径：提醒为"尽力而为"，文档需在提醒页注明"后台提醒存在 15 分钟级延迟，请保持应用在最近任务中"。

### 首次使用流程（新增）

无账号、无服务器，首次启动需引导完成初始化：

1. 欢迎页：一句话说明产品（本地、免费、无账号）
2. 选择关注的行情：Au9999 / 浙商积存金（可全选）
3. 录入首笔持仓：克重 + 买入单价 + 买入日期（或"暂不录入，先进来逛逛"）
4. 请求通知权限（用于价格提醒；拒绝不阻塞使用）
5. 进入首页

## 九、数据库设计（重构）

## gold_price

黄金行情表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| code | TEXT | 行情代码（SGE-Au(T+D) / CZB-JCJ） |
| price | REAL | 最新价格（元/g） |
| change | REAL | 涨跌额 |
| percent | REAL | 涨跌幅（%） |
| pre_close | REAL | 上日收盘价 |
| time | INTEGER | 时间戳（毫秒） |

约束：

-   `UNIQUE(code, time)`：同一行情代码同一时刻只存一条，防重复写入
-   按 `code + time` 建索引，支撑走势图查询
-   滚动清理超过 1 年的数据

## holding

持仓表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键 |
| name | TEXT | 持仓名称（如"浙商积存金"） |
| kind | TEXT | 类型（au9999 / accumulation） |
| amount | REAL | 当前持有克重（含生息） |
| total_cost | REAL | 累计买入总成本（元） |
| created_at | INTEGER | 创建时间 |

> 平均成本不再单独存字段，由 `total_cost ÷ amount` 实时计算，避免生息后不同步。

## trade_record

交易记录表（原 buy_record 重构为事件表）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键（原版缺失） |
| holding_id | INTEGER FK | 所属持仓 |
| type | TEXT | buy（买入）/ sell（卖出）/ interest（生息） |
| amount | REAL | 克重 |
| price | REAL | 成交价（生息事件为 0） |
| fee | REAL | 手续费（卖出按金额 0.4%） |
| time | INTEGER | 交易时间戳 |

## alert

提醒表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 主键（原版缺失） |
| type | TEXT | price_up / price_down / profit_target |
| target | REAL | 目标价或目标金额 |
| enable | INTEGER | 是否启用（0/1） |
| trigger_count | INTEGER | 已触发次数 |
| last_triggered | INTEGER | 最近触发时间 |

## 十、Flutter项目结构

    goldpulse/
    lib/
    ├── main.dart
    ├── app.dart                  # 应用入口 / 主题 / 路由
    ├── pages/
    │   ├── home_page.dart
    │   ├── market_page.dart
    │   ├── asset_page.dart
    │   ├── alert_page.dart
    │   ├── setting_page.dart
    │   └── onboarding_page.dart  # 首次引导（新增）
    ├── widgets/
    │   ├── gold_card.dart
    │   ├── profit_card.dart
    │   └── chart.dart
    ├── models/                   # 数据模型（对应四张表）
    ├── services/
    │   ├── price_api.dart        # 行情接口适配层（主/备源、降级）
    │   ├── calculator.dart       # 资产计算（生息/手续费/平均成本）
    │   ├── alert_service.dart    # 提醒轮询与通知
    │   └── backup_service.dart   # 导出/导入（JSON）
    ├── database/                 # sqflite + 迁移
    ├── state/                    # Riverpod providers（行情/持仓/提醒）
    ├── utils/                    # 格式化（千分位/精度）、交易时段判断
    └── constants/                # 颜色、行情代码映射

## 十一、视觉设计规范

主题：

黑金科技风

颜色：

| 用途 | 色值 |
|------|------|
| 背景 | `#101216` |
| 卡片 | `#181B22` |
| 黄金 | `#D9A441` |
| 上涨 | 红色（如 `#E5484D`） |
| 下跌 | 绿色（如 `#2E9E6B`） |
| 次要文字 | `#8A8F98` |
| 离线/过期降级 | 金色 `#D9A441` 降为 `#6B7280` |

数据规范（新增）：

-   金价：2 位小数 + "元/g"；金额千分位 + "元"
-   克重：展示 2 位小数，内部存储与计算用 4 位（0.0001 克）
-   涨跌百分比：2 位小数 + 括号；正数前显 "▲"、负数显 "▼"
-   大金额（≥ 1 万）可选展示"万"单位（如 39.2 万元），设置页可切换

重点：

大数字展示黄金价格和收益。

## 十二、MVP开发计划（修正阶段划分）

> 原版矛盾：阶段一录入持仓却把 SQLite 放阶段二。持仓不持久化即无意义，修正如下。每个阶段以"可用的纵向切片"交付。

### 第一阶段（MVP，可安装验证）

-   App 骨架 + 黑金主题 + 底部导航 5 页
-   SQLite 建库 + 迁移框架（尽早引入）
-   Au9999 行情展示（主数据源 + 缓存降级）
-   持仓录入（含持久化）+ 资产页交易记录（买入/卖出）
-   资产计算模块：当前价值 / 浮动收益 / 收益率 / 平均成本（含卖出手续费）
-   首页 Dashboard 数据串联
-   首次引导流程 + 空状态

### 第二阶段

-   历史行情持续采集 + 交易时段状态机
-   提醒模块：前台命中通知 + WorkManager 后台轮询
-   数据导出/导入备份
-   交易记录"生息"事件支持 + 平均成本自动摊薄

### 第三阶段

-   K线图（1日/7日/30日，1年随数据积累）
-   备用行情源接入与自动降级完善
-   APK 发布（签名、渠道、混淆）
-   性能优化（列表、图表、启动速度）

## 十三、未来扩展

V1.5：

-   收益曲线（净收益 vs 浮盈）
-   黄金趋势评分
-   更多银行积存金（民生/工商/广发/兴业，接口已具备）

V2.0：

-   多黄金账户
-   黄金ETF / 纸黄金
-   积存金生息自动估算（录入年化利率）

V3.0：

-   AI黄金日报（本地大模型）
-   行情分析（均线/RSI 等指标）
-   Windows / iOS 桌面版
-   多语言支持

## 风险与边界（新增）

| 风险 | 影响 | 对策 |
|------|------|------|
| 免费接口失效 | 无行情数据 | 主/备源 + 本地缓存 + 适配层单点维护 |
| 非官方接口字段变动 | 解析失败 | 适配层收敛 + 版本号预留 |
| 后台提醒精度受限 | 提醒延迟 15 分钟级 | 前台任务可靠性分级 + 用户引导 |
| 本地数据丢失 | 持仓记录丢失 | 导出/导入备份 + 存储目录引导 |
| 行情接口合规性 | 接口可能随时被关闭 | 文档注明仅限个人学习使用 |

## 总结

金脉 GoldPulse 是一个：

> 简洁、私有、本地化的黄金资产监控终端。

打开 App：

看到黄金价格，看到涨跌，看到自己的收益。

这就是金脉的核心价值。
